# Executando o projeto

Para compilar o programa basta estar no diretório pai (trabalho1) e rodar o comando `make`

Será gerado um arquivo executável `romano`, e para testar uma conversão de romano para decimal, basta rodar o comando `./romano <string_numero_romano>`
