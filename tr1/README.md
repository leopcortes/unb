Compilação do projeto:

- Rodar o comando `make` na diretório raiz do projeto

Execução do projeto:

- Rodar o comando `./simulador` no terminal
- Digitar uma mensagem a ser transmitida
- Selecionar uma opção de codificação
- Analisar os resultados da transmissão no promt do terminal
